using System;

class Program
{
    static void Main()
    {
        // Get the bitness of the operating system
        string bitness = Environment.Is64BitOperatingSystem ? "64-bit" : "32-bit";
        
        Console.WriteLine($"Your operating system is {bitness}.");
    }
}
